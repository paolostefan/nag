#include "editor/scene_editor_ui.h"

/**
 * Entry point for the scene editor application.
 * Initializes the SceneEditorUI and starts the main event loop.
 */
int main(int /* argc */, char ** /* argv */) {
  return SceneEditorUI().run();
}
